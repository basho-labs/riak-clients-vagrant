---
- hosts: localhost
  become: yes
  become_method: sudo
  roles:
    # comment out the environments you don't want
    - { role: php }
    - { role: golang }
    - { role: java }
    - { role: nvm }
    - { role: ruby }
    #- { role: kv_testing }
    - { role: ts_testing }
  vars:
    # the vars section is used to pick the settings you want for your local environment
    # see roles/{role_name}/defaults/main.yml for settings you can override
